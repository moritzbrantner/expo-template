name: Mobile analysis

on:
  workflow_dispatch:
  workflow_run:
    workflows:
      - GitHub Pages
    types:
      - completed
    branches:
      - main

jobs:
  analyze:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    uses: moritzbrantner/mobile-analysis/.github/workflows/analyze.yml@2744455defb1315845f895008b8c5fc148e20617
    with:
      target_url: https://moritzbrantner.github.io/expo-template/
      config_path: mobile-analysis.config.json
      run_unlighthouse: true
