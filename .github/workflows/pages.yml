name: GitHub Pages

on:
  pull_request:
    paths:
      - app/**
      - assets/**
      - components/**
      - constants/**
      - hooks/**
      - lib/**
      - packages/**
      - apps/**
      - portfolio/**
      - scripts/build-pages.mjs
      - mobile-analysis.config.json
      - app.config.ts
      - app.json
      - package.json
      - bun.lock
      - tsconfig.json
      - .github/workflows/pages.yml
  push:
    branches:
      - main
    paths:
      - app/**
      - assets/**
      - components/**
      - constants/**
      - hooks/**
      - lib/**
      - packages/**
      - apps/**
      - portfolio/**
      - scripts/build-pages.mjs
      - mobile-analysis.config.json
      - app.config.ts
      - app.json
      - package.json
      - bun.lock
      - tsconfig.json
      - .github/workflows/pages.yml
  workflow_dispatch:

concurrency:
  group: github-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.12

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build portfolio and Expo web apps
        run: node scripts/build-pages.mjs

      - name: Start Pages-compatible preview
        run: |
          mkdir -p .mobile-preview
          ln -s "$GITHUB_WORKSPACE/_site" .mobile-preview/expo-template
          python3 -m http.server 4173 --bind 127.0.0.1 --directory .mobile-preview > /tmp/expo-template-preview.log 2>&1 &
          for attempt in {1..40}; do
            if curl --fail --silent http://127.0.0.1:4173/expo-template/ > /dev/null; then
              exit 0
            fi
            sleep 0.25
          done
          cat /tmp/expo-template-preview.log
          exit 1

      - name: Analyze exact Pages artifact on mobile
        uses: moritzbrantner/mobile-analysis@2744455defb1315845f895008b8c5fc148e20617
        with:
          target_url: http://127.0.0.1:4173/expo-template/
          config_path: mobile-analysis.config.json
          output_dir: mobile-analysis-output
          run_unlighthouse: 'true'

      - name: Upload mobile analysis evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: mobile-analysis-pages
          path: mobile-analysis-output
          if-no-files-found: warn
          retention-days: 14

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4
        with:
          path: _site

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Configure Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
