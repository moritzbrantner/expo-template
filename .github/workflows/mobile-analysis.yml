name: Mobile analysis

on:
  workflow_dispatch:
  workflow_run:
    workflows:
      - GitHub Pages
    types:
      - completed
    branches:
      - main

jobs:
  analyze:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    uses: moritzbrantner/mobile-analysis/.github/workflows/analyze.yml@aa8be6fbaf2f748e8366f26117f1f60d66af5a27
    with:
      target_url: https://moritzbrantner.github.io/expo-template/
      config_path: mobile-analysis.config.json
      run_unlighthouse: true
