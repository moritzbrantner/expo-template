services:
  dev-api:
    build:
      context: ./services/dev-api
    ports:
      - '4402:4002'

  auth-api:
    build:
      context: .
      dockerfile: services/auth-api/Dockerfile
    environment:
      PORT: 4001
      CORS_ORIGIN: '*'
      SMTP_HOST: mailpit
      SMTP_PORT: 1025
      SMTP_FROM: noreply@example.test
      DATA_FILE: /data/users.json
      AUTH_API_ADMIN_EMAILS: admin@example.test
      AUTH_API_ENABLE_TEST_ENDPOINTS: 'true'
    ports:
      - '4401:4001'
    volumes:
      - auth-api-data:/data
    depends_on:
      - mailpit

  mailpit:
    image: axllent/mailpit:latest@sha256:74d609a42ec279aa63c6b4622a6fa9b5408d1ad5b1d76a1c4be40a265ce0863d
    ports:
      - '8825:8025'

volumes:
  auth-api-data:
