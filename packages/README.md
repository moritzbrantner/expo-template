# Shared mobile packages

`packages/*` contains reuse that has been proven by concrete portfolio apps. Shared packages own cross-app mechanics only; product/domain policy stays in the consuming app.

Promotion rule: extract a package after at least two real consumers demonstrate the same stable seam. Do not add speculative capability packages solely because a future app might need them.
